A3 by GRLMAT001:

use "make release" to run program and use command line like so: ./driver test.txt output.txt

to run unit tests, use "make test" and run like so: "./driver" with no command line arguments